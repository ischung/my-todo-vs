{
  "name": "e2e",
  "version": "0.1.0",
  "private": true,
  "description": "Playwright E2E tests — targeting locally-running Docker container",
  "scripts": {
    "test": "playwright test",
    "test:smoke": "playwright test --grep @smoke",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0"
  }
}
